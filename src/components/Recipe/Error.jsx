import { ErrorContainer, ErrorImage } from "../styles/Error.styled";

const Error = () => {
  return (
    <ErrorContainer>
      <ErrorImage></ErrorImage>
    </ErrorContainer>
  );
};

export default Error;
